/* eslint-disable no-magic-numbers */
const config = {
	numberStart: 0,

	calculatorKeys: [

		{ label: { keys: ['Ac', '/', 'X', '%'], values: [] }},
		{	label: { values: [7, 8, 9], keys: ['+'] }},
		{	label: { values: [4, 5, 6], keys: ['-'] }},
		{ label: { values: [1, 2, 3], keys: ['*'] }},
		{ label: { values: [0], keys: ['00', '.', '='] }},
	],
};

export default config;
